spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gym_managementDB?useSSL=false&serverTimezone=UTC
    username: root
    password: '12345'
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
